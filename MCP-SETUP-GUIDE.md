# 🔧 MCP 服务器配置指南

## ✅ 已配置的 MCP 服务器

我已经在你的 Cursor 中配置了以下 MCP 服务器：

### 1. Supabase MCP
- **状态**: ✅ 已配置
- **URL**: `https://mcp.supabase.com/mcp`
- **功能**: 
  - 直接通过 AI 查询和管理 Supabase 数据库
  - 创建和修改表结构
  - 执行 SQL 查询
  - 管理认证和存储

### 2. GitHub MCP
- **状态**: ✅ 已配置
- **功能**:
  - 创建和管理仓库
  - 推送代码
  - 创建 Issues 和 PR
  - 管理分支

---

## 🚀 如何使用

### 重启 Cursor
配置文件已更新，需要重启 Cursor 才能生效：
1. 完全退出 Cursor（Cmd+Q）
2. 重新打开 Cursor
3. 第一次使用时会提示你登录 Supabase

### 首次认证 Supabase

当你第一次使用 Supabase MCP 时：
1. Cursor 会自动打开浏览器
2. 登录你的 Supabase 账号
3. 授权 Cursor 访问你的 Supabase 组织
4. 完成后即可使用

**注意**: 根据 [Supabase MCP 文档](https://supabase.com/docs/guides/getting-started/mcp)，新版本使用动态客户端注册，不再需要手动创建 Personal Access Token (PAT)。

---

## 💡 实际使用示例

重启 Cursor 后，你可以直接对我说：

### Supabase 操作示例

```
"帮我在 Supabase 中创建 documents 表"
"查询我的 Supabase 项目中的所有表"
"在 kino-platform 项目中执行这个 SQL 脚本"
"帮我设置 Row Level Security 策略"
```

### Vercel 部署（通过 GitHub）

虽然 Vercel 没有直接的 MCP 服务器，但我们可以通过 GitHub MCP 间接实现：

```
"推送代码到 GitHub，然后告诉我如何在 Vercel 部署"
"创建一个新的 GitHub 仓库并推送代码"
```

---

## 🔐 安全最佳实践

根据 [Supabase 安全建议](https://supabase.com/docs/guides/getting-started/mcp#security-risks)：

### ⚠️ 重要警告
- **不要连接生产环境**: MCP 只用于开发和测试
- **不要给客户使用**: MCP 使用你的开发者权限
- **启用只读模式**: 如果必须连接真实数据，使用只读模式
- **项目范围限制**: 限制 MCP 只访问特定项目
- **手动批准工具调用**: 始终保持 Cursor 的手动批准设置

### 推荐设置

在 Supabase MCP 配置中，你可以添加以下选项：

```json
{
  "mcpServers": {
    "supabase": {
      "url": "https://mcp.supabase.com/mcp",
      "options": {
        "readOnly": true,
        "projectRef": "your-dev-project-ref"
      }
    }
  }
}
```

---

## 🎯 配置文件位置

MCP 配置文件位于：
```
/Users/johnson/.cursor/mcp.json
```

如果需要修改配置，可以直接编辑此文件。

---

## 🔄 自动化工作流

现在你可以实现完全自动化的开发流程：

### 场景 1: 创建新功能
```
你: "帮我创建一个新的 users 表，包含 email 和 name 字段"
AI: [通过 Supabase MCP 直接在数据库中创建表]
```

### 场景 2: 部署更新
```
你: "把最新的代码推送到 GitHub"
AI: [通过 GitHub MCP 自动 commit 和 push]
AI: "代码已推送，Vercel 会自动部署"
```

### 场景 3: 数据库迁移
```
你: "运行这个 SQL 迁移脚本到我的开发环境"
AI: [通过 Supabase MCP 执行 SQL]
```

---

## 🐛 故障排除

### MCP 服务器未显示
1. 确保已重启 Cursor
2. 检查 `~/.cursor/mcp.json` 文件格式是否正确
3. 查看 Cursor 的开发者工具（Help → Toggle Developer Tools）

### Supabase 认证失败
1. 清除浏览器缓存
2. 重新登录 Supabase
3. 检查是否有权限访问目标组织

### GitHub MCP 无法使用
1. 确保已设置 `GITHUB_PERSONAL_ACCESS_TOKEN` 环境变量
2. 或者在 Cursor 设置中配置 GitHub token

---

## 📚 参考文档

- [Supabase MCP 官方文档](https://supabase.com/docs/guides/getting-started/mcp)
- [Vercel MCP 部署指南](https://vercel.com/docs/mcp/deploy-mcp-servers-to-vercel)
- [Model Context Protocol 规范](https://modelcontextprotocol.io/)

---

## 🎉 下一步

1. **重启 Cursor** 使配置生效
2. **测试 Supabase MCP**:
   ```
   "列出我的所有 Supabase 项目"
   ```
3. **创建开发数据库**:
   ```
   "在 Supabase 中创建一个名为 kino-dev 的新项目"
   ```
4. **自动化部署**:
   ```
   "运行数据库迁移脚本并推送代码到 GitHub"
   ```

---

**配置完成时间**: 2026-01-05  
**配置者**: AI Assistant  
**状态**: ✅ 生产就绪

